# Local development (default): values come from `supabase status` after `supabase start`.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxx

# Remote project (when ready, replace the two values above):
# VITE_SUPABASE_URL=https://<project-ref>.supabase.co
# VITE_SUPABASE_PUBLISHABLE_KEY=<publishable key (sb_publishable_...) from Project Settings → API Keys>

# Cloudflare Turnstile (captcha on the auth forms). Leave unset until
# [auth.captcha] is enabled on the Supabase project — the widget only renders
# when this key is present, and the two must be toggled together.
# VITE_TURNSTILE_SITE_KEY=<site key from the Cloudflare Turnstile dashboard>
