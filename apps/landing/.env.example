# Base URL of the deployed web app. The CTA links to `${PUBLIC_WEB_APP_URL}/sign-up`
# and the footer to `/privacy` and `/terms`.
PUBLIC_WEB_APP_URL=https://app.rinciku.com

# Optional: public repo link shown in the footer.
# PUBLIC_GITHUB_URL=https://github.com/your-org/rinciku
