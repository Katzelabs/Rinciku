# Expo reads variables prefixed with EXPO_PUBLIC_* and inlines them into the
# client bundle at build time (the RN equivalent of the web app's VITE_*).
# Copy to `.env` and fill in. Never commit real values.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
