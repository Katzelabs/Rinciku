import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function NewExpenseScreen() {
  return (
    <PlaceholderScreen
      title='Add expense'
      subtitle='The add-expense form (launched from the glass + button) will live here.'
    />
  );
}
