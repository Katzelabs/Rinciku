// Shared taxonomy types + grouping (portable, from @rinciku/domain).
export {
  groupByTier,
  CATEGORY_ICONS,
  PRESET_COLORS,
  MAX_TIERS,
  MAX_CATEGORIES_PER_TIER,
  type Tier,
  type TierGroup,
  type CategoryIconName,
  type PresetColor,
} from '@rinciku/domain/categories';

export type { Tables } from '@rinciku/db';
